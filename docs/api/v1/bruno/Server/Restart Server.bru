meta {
  name: Restart Server
  type: http
  seq: 11
}

put {
  url: {{url}}:{{port}}/api/v1/server/restart/{{game_uid}}.{{server_name}}
  body: none
  auth: inherit
}

headers {
  X-Api-Key: {{api-key}}
}

settings {
  encodeUrl: true
}
