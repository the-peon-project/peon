meta {
  name: Start Server
  type: http
  seq: 10
}

put {
  url: {{url}}:{{port}}/api/v1/server/start/{{game_uid}}.{{server_name}}
  body: none
  auth: inherit
}

headers {
  X-Api-Key: {{api-key}}
}

settings {
  encodeUrl: true
}
