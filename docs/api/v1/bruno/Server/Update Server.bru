meta {
  name: Update Server
  type: http
  seq: 5
}

put {
  url: {{url}}:{{port}}/api/v1/server/update/{{game_uid}}.{{server_name}}
  body: json
  auth: inherit
}

headers {
  X-Api-Key: {{api-key}}
}

body:json {
  {
      "mode" : "{{update_mode}}"
  }
}
